# TypeScript Blackbook — Claude Code

Shared agent defaults for this repository live in [AGENTS.md](AGENTS.md). Read that file for the full, tool-agnostic conventions.

**Default implementation target:** When the user does not specify a path or app, implement changes in **`apps/v2`** (the Astro v2 site; path is `apps/v2`, not `app/v2`).
