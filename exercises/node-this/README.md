# Exercise: `this` in Node (CommonJS)

This snippet logs `this`, `module`, `exports`, and `global` in a **CommonJS** file.

## Run locally

```sh
cd exercises/node-this
node main.cjs
```

## Open in StackBlitz

Use the GitHub import URL (replace branch if needed):

`https://stackblitz.com/github/unional/typescript-blackbook/tree/main/exercises/node-this`
