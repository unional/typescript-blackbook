/**
 * Primary section links shown in the site header (alongside search / theme).
 * Add entries here when introducing new top-level areas (e.g. changelog, recipes).
 */
export const primaryNav = [
	{ label: 'Blog', path: 'blog' },
	{ label: 'Blackbook', path: 'docs' },
] as const
